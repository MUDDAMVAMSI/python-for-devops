import os
print(os.getenv("PATH")) 
print(os.getenv("PUBLIC"))

# you can check env variables in terminal.just enter "env"

#USER defined env variables below. need to enter below export command in terminal.

# export password="Vamsi Billionaire"
 
print(os.getenv("password"))